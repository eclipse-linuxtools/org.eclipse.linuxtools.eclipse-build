<project name="Pdebuild bootstrap" default="genBuild" basedir=".">
	<property name="javacFailOnError" value="true" />
	<property file="test.properties" />
	<target name="genBuild">
		<echo message="About to generate feature" />
		<exec executable="../../prepare_elements.sh"
		      outputproperty="plugins"
		      dir="./build/plugins" />
		<eclipse.generateFeature featureId="${featureId}"
		                         buildDirectory="${buildDirectory}"
		                         baseLocation="${baseLocation}"
		                         pluginList="${plugins}"
		                         includeLaunchers="false" />
		<echo message="About to generate build.xml" />
		<eclipse.buildScript elements="${plugins}"
		                     configInfo="*,*,*"
		                     buildingosgi="true" />
	</target>

</project>